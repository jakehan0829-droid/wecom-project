# 详情页继续增强：候选 top1/top2 层级 / 消息第二个快捷动作 / 后端编译验证 v11

更新时间：2026-03-30

## 本轮新增

### 1. patient 候选 top1/top2 显式层级
- 候选区已按 `matchScore` 真排序
- 本轮继续增加：
  - TOP 1 / TOP 2 标记
  - top1 / top2 差异化背景与边框

### 2. message 第二个快捷动作
- 在原有 `只看可判断 + 文本 + 最近5条` 基础上
- 新增：`非重复文本判断视图`
- 用于高重复场景下快速切到更适合阅读的消息视图

### 3. 后端编译验证
- 在本轮前端增强外，补一次后端 build 验证
- 用于确认 `operatorName` 链路的 controller / service / audit service 改动已通过类型编译

## 一句话结论

详情页继续往“更明确的候选优先级、更直接的消息判断入口、前后端链路更稳”的方向推进。
