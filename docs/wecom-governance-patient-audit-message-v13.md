# 详情页继续增强：真实治理动作联调结果 / 去重标记样式 / top 摘要收短 v13

更新时间：2026-03-30

## 本轮新增

### 1. 真实治理动作联调已尝试
- 已对 `wecom:private:mapping-chat-demo-001` 发起一次真实 `reassign` 请求
- 请求成功，治理动作已落库
- 使用参数：
  - `operatorName: HanCong`
  - `operatorNote: operatorName联调验证`

### 2. 当前联调发现
- audit 查询返回仍是旧结构中的 `payload_json`
- 本轮新补的 `operatorName` 未在当前运行服务返回中体现
- 这说明：
  - 要么当前本地 3000 服务还没加载到最新后端代码
  - 要么实际运行的是旧进程/旧产物
- 结论：代码链路已改、build 已过、真实请求已打，但还差“重启到最新服务并再次回查”这一步才能确认 operatorName 真正出现在 audit 返回里

### 3. 前端补充
- 去重消息增加高亮样式位
- top1 / top2 推荐摘要继续保留，帮助快速扫读

## 一句话结论

已经完成一次真实治理动作联调，验证了动作链路可用；当前剩余关键点是让运行中的后端服务切到最新代码，再确认 operatorName 在 audit 返回中真实出现。
