# 详情页继续增强：消息建议动作 / 候选理由轻排序 / audit 责任链占位 v9

更新时间：2026-03-30

## 本轮新增

### 1. 消息异常信号 -> 建议动作
- 在异常信号摘要下新增“建议动作”提示
- 当前会根据高噪音 / 高重复 / 缺少可判断文本给出不同建议

### 2. patient 候选理由轻排序感
- 为候选项增加轻量 `matchScore`
- 当前按分组理由数量加权：
  - 身份一致 > 会话相关 > 命中线索
- 先作为前端可解释排序感展示，后续可继续演进为真实排序逻辑

### 3. audit 时间线补责任链占位
- 后端当前未返回独立 operator 字段
- 前端已在时间线卡片中预留“责任链”展示位，并兼容从 detail / payload 中取 operatorName 或 operator
- 当前拿不到时显示 `-`

## 一句话结论

详情页继续往“给出判断建议、表达匹配强弱、预留责任链”的正式治理后台方向推进。
